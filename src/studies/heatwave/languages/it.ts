import { LanguageHelpers } from "../../common/languages/languageHelpers";

import heatwave from "./it.json";

const languageId = "it";

LanguageHelpers.addLanguage(languageId, heatwave, "heatwave/it");
